<!DOCTYPE html>
<html lang="fr">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>{{ website_title }}</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 40px;
        }
        
        .container {
            max-width: 800px;
            margin: 0 auto;
        }
        
        .header {
            background-color: #f4f4f4;
            padding: 20px;
            border-radius: 5px;
        }
    </style>
</head>
<body>
    <div class="container">
        <div class="header">
            <h1>{{ website_title }}</h1>
            <p>Serveur configure automatiquement avec Ansible</p>
            <p>Hote: {{ inventory_hostname }}</p>
            <p>Date de deploiement: {{ ansible_date_time.date }}</p>
        </div>
    </div>
</body>
</html>